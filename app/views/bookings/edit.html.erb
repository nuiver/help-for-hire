<h1>Editing Booking</h1>

<% if can? :update, Booking %>
  <%= render 'form', booking: @booking %>

  <%= link_to 'Show', @booking %> |
<% else %>
  <p>Sign in to edit a booking</p>
<% end %>

<%= link_to 'Back', bookings_path %>
